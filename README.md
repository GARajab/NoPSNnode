# PS4 Installer Node.js Project

1. Edit `config.json` to match your local network, package, and payload.
2. Place `payload.bin` in the project folder.
3. Make sure your NPX HTTP server is running and serving your `.pkg` file.
4. Run:
   npm install
   node index.js
5. Follow terminal instructions.
